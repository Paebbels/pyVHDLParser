Stream of Tokens
################

In the :ref:`first pass <concept-passes>` a source file is sliced into a chain
of double-linked objects of base-class :class:`Token`. While token creation, the
start and end position of a token is preserved as a :class:`SourcePosition`
object within each token.



.. todo::
   Describe the stream of tokens.
